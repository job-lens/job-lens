# 本次交付验证记录

日期：2026-09-19（UTC+8）。对象：JL-SAD-001 v0.1 Proposed，关联 #5 / #6。

## 1. 验证范围和基线

仓库业务基线为 `main@f1072e895d74a963740d46f82ab3e97034376306`，检查前尚无应用实现。本次只新增提案目录下的设计说明、机器契约和静态验证工具；没有运行真实 API、数据库、文件扫描器、SSE 或客户端应用，也没有调整仓库贡献规范。

本地被验证的 OpenAPI 与已提交文件使用 Git blob SHA 比对一致：

```text
openapi.yaml bytes: 45385
Git blob SHA-1: a71a31700e083d6705a09f8494f1c1c087117630
SHA-256: f749eecf48bd3ae75cbe26408df291eefb673ff330e6ab73280c7ba0241d1eb3
```

这些摘要用于确认检查对象，不意味着内容已经得到人工批准。修改契约后必须重新执行检查，旧结果不可沿用。

## 2. 实际执行的静态检查

运行命令：

```bash
python docs/proposals/architecture-v0.1/validate_contract.py
```

本次环境：Python 3.13.5，PyYAML 6.0.3，jsonschema 4.26.0，referencing 0.37.0，FastAPI 0.128.2，Pydantic 2.13.4。

| 检查项 | 实际结果 | 解释 |
| --- | --- | --- |
| YAML 读取及选择的 OAS 版本 | PASS | OpenAPI 3.1.1；锚点展开后检查。 |
| 路径 / 操作 / 模型计数 | 44 / 55 / 63 | operationId 无重复，路径参数与模板对应。 |
| 本地 `$ref` 解析 | 369 处全部通过 | 按展开对象遍历；是引用出现次数，不是不同模型数量。 |
| JSON Schema 结构 | PASS | 逐个检查 Draft 2020-12 Schema；对象拒绝未声明字段，必填字段有定义。 |
| 写操作约定 | PASS | 检查 CSRF、业务 POST 幂等键、需要并发控制的 If-Match 及 412/428 声明。仅检查声明，不是安全行为测试。 |
| Schema 接受样例 | 18 个按预期被接受 | 包括一个明确展示矩形跨字段求和限制的样例，见下文；不能称为 18 个业务成功用例。 |
| Schema 拒绝样例 | 11 个按预期被拒绝 | 包含额外 role、越界音量、非法日期/状态、返工步骤冲突、缺少取消理由、空消息及错误序号类型。 |
| FastAPI OpenAPI 结构模型 | PASS | `OpenAPI.model_validate` 通过；不是应用路由执行。 |

特别说明：`x=0.9,width=0.3` 的矩形虽然每个值都在 0–1 内，但总和超过图像边界。验证脚本故意保留该例，证明普通字段 Schema 不足以保证几何语义；实际服务必须另外检查 `x+width<=1` 和 `y+height<=1`。这不是一个被误标为可接受业务标注的测试。

当前环境没有安装完整的外部 OpenAPI 合规验证器。因此结论限定为“结构、引用、示例和项目约定检查通过”，不宣称通过全部 OpenAPI 标准认证。实现前还应在团队 CI 中加入批准的 lint、生成与运行时契约差异检查。

## 3. 文档与来源复核

已阅读三份业务材料及当前 README、CONTRIBUTING、Proposal/PR 模板。文档区分原文要求、工程建议、长期方向和待产品决策；排版样例仅作为版式依据，不引入其科研、大模型或知识库业务。

架构文档记录 15 项需求追踪、10 项 ADR 和 10 项待决策；首版范围、真实通话、SOP 转换/AR、精确位置、企业报告等都保留出处和决定门槛。没有将原型中的姓名、文件、百分比或训练时长示例当作真实用户资料或算法效果。

接口会评仍有一项明确字段补齐：授权辅导员读取学员界面偏好的响应投影，当前 `Profile` 未包含该字段。具体建议和冻结前要求记录在 [README 的契约门槛](README.md#契约理解与冻结门槛)，不将此项标记为已经覆盖完成。

## 4. Word 格式和逐页检查

单独交付文件：`融职境_系统架构设计文档_v0.1_评审稿.docx`。

- 本次最终渲染为 **48 页**，包含 **41 张表格、5 张架构/关系/流程示意图**。
- 采用样例的 A4、左右 3 cm/上下 2.54 cm 页边距、黑体标题、宋体正文、1.5 倍正文行距、首行缩进、三线表和居中页码；增加可点击章节导航及技术接口附录。
- 已渲染为逐页 PNG，并逐页打开检查第 1–48 页；修复过封面后的多余空白页、默认模板色线/斜体样式以及代码片段分页问题。
- 最终检查未见文字/图表裁切、重叠、缺字或额外空白页；跨页表格重复表头，四人主责表保持在同一页。
- Word 是排版阅读版，不是仓库 Markdown 的逐字镜像；机器字段与操作附录由本次 OpenAPI 提取，精确实现规则仍须结合仓库契约及后续人工决议。
- 不同阅读器和字体替换可能改变分页；本记录的 48 页指本次交付渲染环境。未嵌入或分发字体文件。

```text
DOCX bytes: 717832
DOCX SHA-256: 3aa31e13c6b358c7d5aab63b79ee570f40c081346922c95c3ce2d807c0f56b38
```

Word 通过会话附件交付；源 PRD、原排版样例和中间渲染文件没有上传公开仓库。

## 5. 未执行且不得标为通过的验证

| 项目 | 当前状态 / 原因 |
| --- | --- |
| 应用启动、前后端单元及端到端测试 | Not run；没有业务实现。 |
| 角色/资源授权、CSRF 与会话撤销行为 | Not run；目前只有协议和测试要求。 |
| 数据库迁移、事务竞争、重复发布/提交 | Not run；待真实 PostgreSQL 与服务实现。 |
| 恶意文件隔离、扫描、私有下载 | Not run；待存储/扫描适配与测试材料。 |
| SSE 顺序、断线补拉、轮询与后台恢复 | Not run；待完整双端和数据库通知实现。 |
| 性能 p95、错误率、连接容量 | Not run；文中的数字是拟验收目标。 |
| 备份恢复、RPO/RTO | Not run；文中的目标需要演练验证。 |
| 设备、可访问性、真实通话与跨端平台 | Not run；需确定平台、账号、SDK 和真机。 |
| 产品边界、隐私与四位成员批准 | Pending human review；没有代签、自动批准或标记 Proposal-Accepted。 |

## 6. 下一阶段门槛

先由刘峥岩组织范围和架构评审，贺凡恩及两名前端复核数据、接口、字段补齐与工作量。定稿结论保留在 #5，本设计 PR 依仓库规则不直接合并。随后另开空骨架 Task/PR，并在其合并后逐条实现已批准业务流程。当前验证不能跳过这些门槛。
